{
  "name": "graphql-query-vars-cleaner",
  "version": "1.4.1",
  "description": "Remove undefined variables from a GraphQL query",
  "main": "./build/index.js",
  "author": "Connor White",
  "license": "MIT",
  "scripts": {
    "build": "rm -r build && tsc --project ./tsconfig.json",
    "test": "yarn run build && node ./test.js",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "graphql-tag": "^2.10.1",
    "json-to-graphql-query": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  },
  "peerDependencies": {
    "graphql": "^14.6.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ConnorWhite/graphql-query-vars-cleaner"
  },
  "files": [
    "build/*"
  ]
}
